// Code generated by protoc-gen-strict. DO NOT EDIT.
// source: shop/coverage/v1/rules.proto

import type { Email, Narrow, NonEmptyList, Require, Uuid } from "../../../strict/types";
import type { AmbiguityCoverage, CelNarrowingCoverage, CelNarrowingCoverage_Inner, CelRuleCoverage, CollectionRuleCoverage, Flavor, NumericRuleCoverage, PresenceRuleCoverage, ScalarRuleCoverage, StringRuleCoverage, WellKnownRuleCoverage } from "./rules_pb";
import { AmbiguityCoverageSchema, CelNarrowingCoverageSchema, CelNarrowingCoverage_InnerSchema, CelRuleCoverageSchema, CollectionRuleCoverageSchema, CoverageService, PresenceRuleCoverageSchema, ScalarRuleCoverageSchema, StringRuleCoverageSchema } from "./rules_pb";
import { EmptySchema } from "@bufbuild/protobuf/wkt";
import type { GenMessage, GenService } from "@bufbuild/protobuf/codegenv2";

/**
 * Flavor exercises the enum rule family.
 *
 * Without FLAVOR_UNSPECIFIED, the member protobuf numbers 0, which this overlay
 * reads as "unset" rather than a value. No buf.validate rule says so; the
 * convention that names it <ENUM>_UNSPECIFIED does.
 */
export type FlavorStrict = Exclude<Flavor, 0>;

/**
 * StringRuleCoverage exercises the string rule family, including the well-known format rules that live in a oneof inside StringRules.
 *
 * Left to runtime validation, having no type equivalent:
 *   exact_len
 *     string.len = 10
 *   len_bounds
 *     string.max_len = 64
 *     string.min_len = 2
 *   byte_bounds
 *     string.max_bytes = 128
 *     string.min_bytes = 2
 *   pattern
 *     string.pattern = ^[a-z]+(_[a-z]+)*$
 *   affixes
 *     string.contains = -
 *     string.not_contains =  
 *     string.prefix = id_
 *     string.suffix = _v1
 *   enumerated
 *     string.in = [alpha, beta, stable]
 *     string.not_in = [deprecated]
 *   constant
 *     string.const = fixed
 *   hostname
 *     string.hostname = true
 *   uri
 *     string.uri = true
 *   uri_ref
 *     string.uri_ref = true
 *   tuuid
 *     string.tuuid = true
 *   ipv4
 *     string.ipv4 = true
 *   ipv6
 *     string.ipv6 = true
 *   ip
 *     string.ip = true
 *   ip_prefix
 *     string.ip_prefix = true
 *   host_and_port
 *     string.host_and_port = true
 *   header_name
 *     string.well_known_regex = KNOWN_REGEX_HTTP_HEADER_NAME
 */
export type StringRuleCoverageStrict = Narrow<StringRuleCoverage, {
  /**
   * string.email = true
   *
   * Carried into the type: string.email.
   */
  email: Email;
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  uuid: Uuid;
}>;

/**
 * NumericRuleCoverage exercises every numeric rule family. protovalidate declares a separate rule message per wire type, so each one is walked separately by the parser.
 *
 * Left to runtime validation, having no type equivalent:
 *   i32
 *     int32.gt = 0
 *     int32.lt = 1000
 *   i64
 *     int64.gte = -9000
 *     int64.lte = 9000
 *   u32
 *     uint32.lte = 65535
 *   u64
 *     uint64.gte = 1
 *   s32
 *     sint32.gte = -128
 *   s64
 *     sint64.lte = 1024
 *   f32
 *     fixed32.gt = 0
 *   f64
 *     fixed64.gt = 0
 *   sf32
 *     sfixed32.lt = 0
 *   sf64
 *     sfixed64.lt = 0
 *   ratio
 *     float.finite = true
 *     float.gte = 0
 *     float.lte = 1
 *   factor
 *     double.in = [0.5, 1, 2]
 *   constant
 *     int32.const = 42
 *   excluded
 *     int32.not_in = [-1, 0]
 */
export type NumericRuleCoverageStrict = NumericRuleCoverage;

/**
 * ScalarRuleCoverage exercises the remaining scalar families.
 *
 * Left to runtime validation, having no type equivalent:
 *   accepted
 *     bool.const = true
 *   payload
 *     bytes.max_len = 4096
 *     bytes.min_len = 1
 *   signature
 *     bytes.len = 32
 *     bytes.prefix = [1 2]
 *   remote_ip
 *     bytes.ip = true
 */
export type ScalarRuleCoverageStrict = Narrow<ScalarRuleCoverage, {
  /** enum.defined_only = true */
  defined: FlavorStrict;
  /**
   * enum.defined_only = true
   * enum.in = [1, 2]
   *
   * Carried into the type: enum.in.
   */
  subset: Extract<FlavorStrict, 1 | 2>;
  /**
   * enum.const = 3
   *
   * Carried into the type: enum.const.
   */
  pinned: Extract<FlavorStrict, 3>;
}>;

/**
 * WellKnownRuleCoverage exercises well-known types and their rule families.
 *
 * Left to runtime validation, having no type equivalent:
 *   timeout
 *     duration.gte.seconds = 1
 *     duration.lte.seconds = 300
 *   interval
 *     duration.in = [{seconds: 60}, {seconds: 300}, {seconds: 900}]
 *   seen_at
 *     timestamp.lt_now = true
 *   expires_at
 *     timestamp.gt_now = true
 *   recent
 *     timestamp.within.seconds = 86400
 *   after_epoch
 *     timestamp.gte = {}
 *   detail
 *     any.in = [type.googleapis.com/google.protobuf.Duration]
 */
export type WellKnownRuleCoverageStrict = WellKnownRuleCoverage;

/**
 * CollectionRuleCoverage exercises repeated and map rules, including rules applied to the elements rather than the collection.
 *
 * Left to runtime validation, having no type equivalent:
 *   counters
 *     map.keys.string.min_len = 1
 *     map.keys.string.pattern = ^[a-z_]+$
 *     map.max_pairs = 50
 *     map.min_pairs = 1
 *     map.values.int32.gte = 0
 *   readings
 *     map.max_pairs = 8
 */
export type CollectionRuleCoverageStrict = Narrow<CollectionRuleCoverage, {
  /**
   * repeated.items.string.min_len = 1
   * repeated.max_items = 10
   * repeated.min_items = 1
   * repeated.unique = true
   *
   * Carried into the type: repeated.min_items.
   */
  names: NonEmptyList<CollectionRuleCoverage["names"]>;
  /**
   * repeated.items.enum.defined_only = true
   * repeated.items.enum.not_in = [0]
   */
  flavors: FlavorStrict[];
  /** repeated.max_items = 4 */
  nested: StringRuleCoverageStrict[];
}>;

/**
 * PresenceRuleCoverage exercises required, optional and the ignore modes that decide when the other rules are evaluated at all.
 *
 * Left to runtime validation, having no type equivalent:
 *   required_value
 *     required
 *     string.min_len = 1
 *   optional_value
 *     string.min_len = 3
 *   skip_when_empty
 *     ignore = IGNORE_IF_ZERO_VALUE
 *     string.uuid = true
 *   never_checked
 *     ignore = IGNORE_ALWAYS
 */
export type PresenceRuleCoverageStrict = Require<Narrow<PresenceRuleCoverage, {
  /**
   * required
   *
   * Carried into the type: required.
   */
  requiredMessage: StringRuleCoverageStrict;
}>, "requiredMessage">;

/**
 * CelRuleCoverage exercises the CEL paths: several rules on one field, the shorthand form, and message-level rules that span fields.
 *
 * Left to runtime validation, having no type equivalent:
 *   cel[cel_coverage.window_ordered]
 *     this.window_end > this.window_start
 *     message: window_end must be after window_start
 *   cel[cel_coverage.slugs_lowercase]
 *     this.slugs.all(s, s == s.lowerAscii())
 *     message: every slug must be lowercase
 *   cel[this.window_start >= 0]
 *     this.window_start >= 0
 *   identifier
 *     cel[cel_coverage.identifier.prefix]: this.startsWith('itm_')
 *       message: identifier must start with 'itm_'
 *       refs: this
 *       calls: startsWith
 *     cel[cel_coverage.identifier.length]: this.size() >= 4 && this.size() <= 40
 *       message: identifier must be 4 to 40 characters
 *       refs: this
 *       calls: _&&_, _<=_, _>=_, size
 *   window_start
 *     cel[this % 15 == 0]: this % 15 == 0
 *       refs: this
 *       calls: _%_, _==_
 *   window_end
 *     int32.lte = 1440
 *     cel[cel_coverage.window_end.quarter_hour]: this % 15 == 0
 *       message: window_end must fall on a quarter hour
 *       refs: this
 *       calls: _%_, _==_
 *   slugs
 *     cel[cel_coverage.slugs.non_empty]: this.all(s, s.size() > 0)
 *       message: slugs must not contain empty entries
 *       refs: s, this
 *       calls: _&&_, _>_, size
 */
export type CelRuleCoverageStrict = Narrow<CelRuleCoverage, {
  /**
   * cel[cel_coverage.nested.uuid_set]: this.uuid != ''
   *   message: nested.uuid must be set
   *   refs: this, uuid
   *   calls: _!=_
   */
  nested: StringRuleCoverageStrict;
}>;

/**
 * CelNarrowingCoverage exercises the CEL shapes that become part of the generated type rather than being left to runtime validation.
 *
 * Carried into the type: cel[cel_narrowing.shapes], cel[cel_narrowing.nested_shapes].
 *
 * Left to runtime validation, having no type equivalent:
 *   cel[cel_narrowing.shapes]
 *     this.label != ""
 *     message: label must be set, slug must be empty, flavor must be unset, note must be absent and detail must be present
 *   cel[cel_narrowing.nested]
 *     this.detail.uuid != ""
 *     message: detail.uuid must be set
 *   cel[cel_narrowing.foreign]
 *     has(this.inner.mask.paths)
 *     message: inner.mask must name at least one path
 */
export type CelNarrowingCoverageStrict = Require<Narrow<CelNarrowingCoverage, {
  slug: CelNarrowingCoverage["slug"] & "";
  flavor: Extract<FlavorStrict, 0>;
  note: never;
  detail: StringRuleCoverageStrict;
  inner: Require<Narrow<CelNarrowingCoverage_InnerStrict, { stamp: NonNullable<CelNarrowingCoverage_InnerStrict["stamp"]>; flavor: Extract<CelNarrowingCoverage_InnerStrict["flavor"], 0>; }>, "stamp">;
}>, "detail">;

/** Inner holds the fields the nested-shape rule reaches into: a message field, which tracks presence, and an enum. */
export type CelNarrowingCoverage_InnerStrict = Narrow<CelNarrowingCoverage_Inner, {
  flavor: FlavorStrict;
}>;

/**
 * AmbiguityCoverage exercises the cases where the proto text and what the generated code declares come apart: a property named by something other than the field, a rule combination no target can carry, and a rule the overlay has to report rather than translate.
 *
 * Carried into the type: cel[ambiguity.implicit_presence], cel[ambiguity.presence_and_value].
 *
 * Left to runtime validation, having no type equivalent:
 *   cel[ambiguity.implicit_presence]
 *     has(this.label)
 *     message: label must be set and legacy_note must be left empty
 *   cel[ambiguity.presence_and_value]
 *     this.nickname != ""
 *     message: nickname must be set and non-empty
 *   cel[ambiguity.multiline]
 *     this.label != "" || this.legacy_note != ""
 *     message: label and legacy_note must not both be empty
 *   cel[ambiguity.oneof_member]
 *     has(this.email)
 *     message: email must be the channel that is set
 *   email
 *     string.email = true
 *   phone
 *     string.min_len = 5
 *   unchecked_detail
 *     ignore = IGNORE_ALWAYS
 *   outside_window
 *     int32.gt = 20
 *     int32.lt = 10
 *   optional_tags
 *     repeated.items.ignore = IGNORE_IF_ZERO_VALUE
 *     repeated.items.string.min_len = 3
 *   quantity
 *     cel[ambiguity.field_multiline]: this > 0 &&
 * this % 10 == 0
 *       message: quantity must be a positive multiple of ten
 *       refs: this
 *       calls: _%_, _&&_, _==_, _>_
 */
export type AmbiguityCoverageStrict = Require<Narrow<AmbiguityCoverage, {
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  userId: Uuid;
  /**
   * string.uuid = true
   *
   * Carried into the type: string.uuid.
   */
  constructor$: Uuid;
  legacyNote: AmbiguityCoverage["legacyNote"] & "";
  nickname: NonNullable<AmbiguityCoverage["nickname"]>;
}>, "nickname">;

/**
 * Describes shop.coverage.v1.StringRuleCoverage, reporting StringRuleCoverageStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const StringRuleCoverageStrictSchema =
  StringRuleCoverageSchema as GenMessage<StringRuleCoverage, { validType: StringRuleCoverageStrict }>;

/**
 * Describes shop.coverage.v1.ScalarRuleCoverage, reporting ScalarRuleCoverageStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const ScalarRuleCoverageStrictSchema =
  ScalarRuleCoverageSchema as GenMessage<ScalarRuleCoverage, { validType: ScalarRuleCoverageStrict }>;

/**
 * Describes shop.coverage.v1.CollectionRuleCoverage, reporting CollectionRuleCoverageStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const CollectionRuleCoverageStrictSchema =
  CollectionRuleCoverageSchema as GenMessage<CollectionRuleCoverage, { validType: CollectionRuleCoverageStrict }>;

/**
 * Describes shop.coverage.v1.PresenceRuleCoverage, reporting PresenceRuleCoverageStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const PresenceRuleCoverageStrictSchema =
  PresenceRuleCoverageSchema as GenMessage<PresenceRuleCoverage, { validType: PresenceRuleCoverageStrict }>;

/**
 * Describes shop.coverage.v1.CelRuleCoverage, reporting CelRuleCoverageStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const CelRuleCoverageStrictSchema =
  CelRuleCoverageSchema as GenMessage<CelRuleCoverage, { validType: CelRuleCoverageStrict }>;

/**
 * Describes shop.coverage.v1.CelNarrowingCoverage, reporting CelNarrowingCoverageStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const CelNarrowingCoverageStrictSchema =
  CelNarrowingCoverageSchema as GenMessage<CelNarrowingCoverage, { validType: CelNarrowingCoverageStrict }>;

/**
 * Describes shop.coverage.v1.CelNarrowingCoverage.Inner, reporting CelNarrowingCoverage_InnerStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const CelNarrowingCoverage_InnerStrictSchema =
  CelNarrowingCoverage_InnerSchema as GenMessage<CelNarrowingCoverage_Inner, { validType: CelNarrowingCoverage_InnerStrict }>;

/**
 * Describes shop.coverage.v1.AmbiguityCoverage, reporting AmbiguityCoverageStrict as its valid type.
 * The same descriptor protoc-gen-es generated, so the wire format and the identity
 * this has as a query key are unchanged — only what `MessageValidType` reports differs.
 */
export const AmbiguityCoverageStrictSchema =
  AmbiguityCoverageSchema as GenMessage<AmbiguityCoverage, { validType: AmbiguityCoverageStrict }>;

type CoverageServiceStrictDescriptor = GenService<{
  /** Takes a message this run narrows, so the overlay retypes its schema. */
  check: {
    methodKind: "unary";
    input: typeof StringRuleCoverageStrictSchema;
    output: typeof EmptySchema;
  },
  /** Takes and returns a well-known type: nothing narrows it, and no sibling module exports it — protobuf-es ships it in @bufbuild/protobuf/wkt. */
  ping: {
    methodKind: "unary";
    input: typeof EmptySchema;
    output: typeof EmptySchema;
  },
}>;

/**
 * CoverageService exists for the two RPC shapes the overlay has to resolve.
 * The same service descriptor protoc-gen-es generated, retyped so every method reports
 * the strict input and output types.
 */
export const CoverageServiceStrict =
  CoverageService as CoverageServiceStrictDescriptor;

